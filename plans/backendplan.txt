now let's setup a plan for the backend 
  but first, the investment plan modal should be like how the other modals are, we select the plan then       
  we're able to pay and all that                                                                              
  well this is the plan                                                                                       
  the dashboard page cards, will show wallet balance, total amounts we've invested (all time), active investments, expected returns(we replace this with Active Positions for commodities,)
  now we have 4 shortcut cards that all need to work and perfrom their function.
  next page is the wallet page, whenever user wants to deposit we use nowpayments (api keys already active and ready), also when done update the callback url and point out which i'll place in the now payment api link
  as for the linking wallet, when done, toast message is showed wallet linked, then the link wallet card is updated to show wallet is linked and then we can add  a button to link another wallet and view linked wallet (all of these saved to DB and shown in Admin Panel)
  transaction history and the rest you already know how to handle on that page
  the investment page needs its modal to be done, the modal should be like the other commodities modal, where the user can select the plan and then start their investment, the current modal for that page should be discontinued
  now for the commodity sections, when we select a n asset we should always have the live prices of that asset in the modal as well, 
  likewise in the real estate page
  so the workflow is 
  before any investment, commodity or real estate user must have deposited funds,
  now using any of the above user opens modal, selects plan detail , inputs amount then confirms it. after that loader then toast message then email is sent to user, user and admin dashboard should be updated
  email sending is very important, it shows that the platform is professional
  so at the end of this the backend for all processes in the user dashboard should be working fine!
  start up the plan to do this